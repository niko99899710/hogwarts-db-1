1. SELECT name, age,  FROM USER
   WHERE  age > 10 AND  age < 20
   ORDER BY age

2. SELECT name FROM USER

3. SELECT name, COUNT (name) FROM USER
   WHERE name IN("o")
   GROUP BY name

4. SELECT MIN (age), name  FROM USER


5. SELECT FROM USER
   ORDER BY age